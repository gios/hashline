module.exports = function(io, socket) {
  'use strict'

  // const knex = require('../knexConfig')
  // const logger = require('tracer').colorConsole()

  // socket.on('invite users', users => {
  //   io.broadcast('invite users', params.username)
  // })
}